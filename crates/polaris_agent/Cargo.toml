[package]
name = "polaris_agent"
version = "0.0.2"
description = "Agent trait for defining reusable behavior patterns in Polaris."
edition = "2024"
keywords = []
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/RadiantAILabs/polaris"
rust-version = "1.93.0"

[lints]
workspace = true

[dependencies]
polaris_graph = { path = "../polaris_graph", version = "0.1.0" }
polaris_system = { path = "../polaris_system", version = "0.0.3" }
thiserror = "2"
