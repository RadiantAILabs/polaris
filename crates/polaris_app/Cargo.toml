[package]
name = "polaris_app"
version = "0.0.1"
description = "Shared HTTP server runtime for Polaris products."
edition = "2024"
keywords = ["polaris", "http", "axum", "server"]
license = "Apache-2.0"
repository = "https://github.com/RadiantAILabs/polaris"
categories = ["web-programming::http-server", "asynchronous"]

[features]
default = []
otel = []

[lints]
workspace = true

[dependencies]
polaris_system = { path = "../polaris_system" }
polaris_core_plugins = { path = "../polaris_core_plugins" }
axum = { version = "0.8", features = ["tokio"] }
tokio = { version = "1", features = ["rt", "sync", "net", "signal", "macros"] }
tower-http = { version = "0.6", features = ["cors", "trace", "request-id", "util", "propagate-header"] }
tracing = "0.1"
parking_lot = "0.12"
http = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }
